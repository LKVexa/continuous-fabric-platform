# U6 / U7 Fixes Evidence — 2026-09-23

Historical Windows workstation run (host name and user-specific path omitted for publication).
MSYS toolchain on PATH: `C:\msys64\usr\local\bin;C:\msys64\usr\bin;C:\msys64\ucrt64\bin` first.
Canonical `verify --full` finished **2026-09-23 ~14:35 PT** (UTC `2026-09-23T21:35:06Z` in gate JSON).

**Do not claim OPERATIONAL.** Claim boundary unchanged: PHYSICAL_*_QPU_EXECUTION BLOCKED; fabric NETWORK=deny (U5 PASS). hermit / JA21 left alone.

## Backup

`DF_Unified\reports\medium_g0_fix_backup_20260923\`

- Pre-fix seals: per-member MANIFEST / SHA256SUMS / PACORE_DIGEST / fabric.py / gates.py
- Nested Medium copy relocated to `removed_nested_DF_Medium\` (not destroyed)
- Pre-fix UNIFIED.json, NODES.json, prior gate results
- NEW_DIGESTS.json (post-remint pins)

---

## 1) Medium nested extras (U6 G0.1 / G0.2)

### Diagnosis
- On-disk `DF_Medium\DF_Medium\` was a **byte-identical nested duplicate** of the parent seal (SHA256SUMS identical; 365 files).
- UNIFIED already classified it as `kind=nested_duplicate` / `status=DUPLICATE_COPY` with note: not referenced by any launcher.
- No launcher/script references to `DF_Medium/DF_Medium` found.
- Member-native G0 has no UNIFIED overlay exception → 365 unbound extras → G0.1/G0.2 FAIL.

### Action (evidence-backed: accidental copy → remove)
1. Moved nest to backup (`removed_nested_DF_Medium`).
2. Removed the Medium overlay entry from `DF_Unified/UNIFIED.json` (5 → 4 overlays).
3. No Medium-only seal remint required for the delete (nest was never in the sealed inventory). Medium was reminted later only because shared `adapter/` + `core/` changed for other fixes.

### Result
- Medium G0.1 / G0.2 **PASS** on live `node-verify`.
- U3 / U4 **PASS** (no undeclared Medium extras; U4 no longer expects the nest).

---

## 2) Fabric PHOTON / pk overlays (U7 G0.1 / G0.2)

### Diagnosis
- UNIFIED declares four Fabric overlays: `PHOTON/`, `PHOTON.cmd`, `PHOTON.sh`, `pk/` (VENDORED_UNBOUND / APPLIED_UNSEALED — unbound by design).
- Member-native G0 ignored UNIFIED → 824 unbound/extras.

### Action (prefer overlay declaration; do not seal unbound content)
1. Extended shared `adapter/dfabric/gates.py` with `_unified_overlay_prefixes` / `_filter_overlay_extras`.
2. Node + Fabric G0.1/G0.2 now honor sibling `DF_Unified/UNIFIED.json` overlay prefixes (stockpile layout).
3. Reminted all five members after shared adapter/core edits; Fabric reminted with PHOTON/pk temporarily aside so they stay outside the seal (file_count remains **129**).
4. UNIFIED Fabric overlays kept.

### Result
- Fabric G0.1 / G0.2 **PASS** (`unbound_overlay_declared_n` / `extra_overlay_declared_n` = 824).
- U1 / U3 / U4 **PASS**.

---

## 3) Windows fork F5 / F6 (+ CRLF follow-on)

### Diagnosis
- `core/reference/pacore/fabric.py` used `multiprocessing.get_context("fork")` → ValueError on Windows.
- After fork fix + C toolchain bind, F4–F7/F9 then failed on CRLF in adapter-written `.lctlc` / `.mssl` (`open(..., "w")` on Windows).

### Action
1. `fabric.py`: choose `fork` if available else `spawn` (Windows → spawn). Synced to all five `core/` trees; regenerated `PACORE_DIGEST.json`.
2. `nodes.py` + `gates.py`: write guest sources with `newline="\n"`. Synced to all five adapters.
3. Reminted all five seals + UNIFIED pins + `DF_Fabric/fabric/NODES.json` node pins.
4. Built Medium/Large native tools so F1 can bind all four nodes.

### Result
- Fabric battery **16/16 PASS** including **F5 and F6** (no fork error).
- **U7 PASS**.

---

## Final verify --full table (canonical)

| gate | result | notes |
|---|---|---|
| U0 | **PASS** | |
| U1 | **PASS** | pins match live seals + Fabric NODES |
| U2 | **PASS** | shared adapter/core identical |
| U3 | **PASS** | overlays accounted |
| U4 | **PASS** | Fabric overlays present; Medium nest gone |
| U5 | **PASS** | PHYSICAL_* BLOCKED; NETWORK=deny |
| U6 | **FAIL** | see member-native residuals below |
| U7 | **PASS** | F0–F9 all PASS (~184s) |

**Verdict: FAIL** · 7 pass, 1 fail, 0 skipped

### U6 member-native (live --full batteries)

| member | verdict | highlights / residuals |
|---|---|---|
| DF_Small | **PASS** (18/0/0) | G0–G1 + N1 + A* PASS with C toolchain |
| DF_Medium | **FAIL** (17/3/0) | G0 PASS. **N1** own_gate rc=2; **A10/A11** residual |
| DF_Large | **FAIL** (17/1/0) | G0 + A* PASS. **N1** scratch BR-450 suite 2/31 FAIL |
| DF_Xtra_Large | **FAIL** (17/1/1) | G0 PASS. **N1** own_gate rc=127 + vm_verify_test rc=2; **A12 SKIPPED_NO_JDK** |

### U7 Fabric F0–F9

| gate | result |
|---|---|
| G0.1, G0.2, G0.4, G1, G2, G3 | PASS |
| F0, F1, F2, F3, F4, F5, F6, F7, F8, F9 | **PASS** |

Run: `DF_Fabric\_runs\VERIFY_20260923T213202Z.json`

---

## Residual blockers (keep U6 red; outside the three requested areas)

1. **Medium N1 / A10 / A11** — VM own acceptance gate / adapter paths still fail in scratch under the JA21 portable temp root.
2. **Large N1** — BR-450 suite reports 2 failures during scratch rebuild.
3. **Xtra_Large N1** — own_gate exit 127 (command missing in scratch shell) + vm_verify_test rc=2; **A12** needs JDK.
4. Default `status` (no `--full`) still synthesizes U6/U7 SKIPPED — expected.

---

## Evidence paths

- This note: `DF_Unified\reports\U6_U7_FIXES_20260923.md`
- Backup: `DF_Unified\reports\medium_g0_fix_backup_20260923\`
- Full log: `DF_Unified\reports\VERIFY_FULL_U67FIX_20260923.txt`
- Gate JSON/MD: `DF_Unified\reports\UNIFIED_GATE_RESULTS.json` / `.md`
- CFP copy: `CFP_Continuous_Fabric_Platform_v1.0.0\reports\U6_U7_FIXES_20260923.md`
